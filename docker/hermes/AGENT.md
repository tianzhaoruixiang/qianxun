# AGENT.md → 请使用 AGENTS.md

Hermes Agent 官方识别的项目上下文文件名是 **`AGENTS.md`**（复数），不会加载 `AGENT.md`。

本仓库 default（数智干警）的调度手册请编辑：

- `docker/hermes/AGENTS.md`（模板）
- 运行时：`$HERMES_HOME/AGENTS.md`（容器内 `/opt/data/AGENTS.md`）

身份与硬约束在 `SOUL.md`。
